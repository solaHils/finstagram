<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" type="text/css" href="/stylesheets/normalize.css">
        <link rel="stylesheet" type="text/css" href="https://fonts.googleapis.com/css2?family=Patrick+Hand&family=Saira+Condensed:wght@200&display=swap">
        <link rel="stylesheet" type="text/css" href="/stylesheets/lib.css">
        <link rel="stylesheet" type="text/css" href="/stylesheets/app.css">
        <title>Cutestagram</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
    </head>
    <body>
        <header>
            <div class="header-guide">
                <h1>
                    <a href="/">Cutestagram</a>
                </h1>
                <div class="login-info">
                    <% if current_user %>
                        <a href="/logout">Logout</a> &nbsp;
                        <img style="width:35px; border-radius: 50%;" src="<%= current_user.avatar_url %>">
                    <% else %>
                        <a href="/login">Login</a>
                        <a href="/signup">Signup</a>
                    <% end %>
                </div>
            </div>
        </header>
        <main role="main">
            <%= yield %>
        </main>
    </body>
</html>